# 0x862bdc3c Documentation

class:
  description: |
    Parameter [override](/classes/0xa41315c0) that replaces the emitter parameter
    outright: whatever the parameter currently holds is discarded, and the folded
    result of its `Modifiers` chain becomes the value directly, with no transition.
